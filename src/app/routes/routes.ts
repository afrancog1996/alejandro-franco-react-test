import { IAppRoutes } from "../../react-app-env";

const appRoutes: IAppRoutes = {
  MAIN: "/",
  HOME: "/home",
  LOGIN: "/login",
  UPLOAD: "/upload",
  EMPLOYEES: "/employees",
  UNKNOWN: "*",
};

export default appRoutes;